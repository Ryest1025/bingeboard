// client/src/pages/DiscoverLab.tsx
// Lightweight test page that renders the new integrated Discover layout
// Use /discover-lab to preview changes without touching the primary /discover route
import React from 'react';
import DiscoverStructured from '@/pages/DiscoverStructured';

export default function DiscoverLab() {
  return <DiscoverStructured />;
}
