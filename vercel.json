{
  "version": 2,
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "NODE_ENV": "development"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}