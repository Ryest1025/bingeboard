{
  "version": 2,
  "installCommand": "npm install --include=optional",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "NPM_CONFIG_OPTIONAL": "true"
  }
}